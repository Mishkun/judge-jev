# Required by npm run dev:polling
JEV_OPENROUTER_TOKEN=or-replace-me
TELEGRAM_BOT_TOKEN=123456789:replace-me

# Leave unset/0 while a webhook owns this bot. Set to 1 only to intentionally
# delete the active webhook before local polling (pending updates are kept).
POLLING_DELETE_WEBHOOK=0
