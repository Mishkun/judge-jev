{
  "name": "judge-jev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Telegram Guest Mode Cloudflare Worker backed by OpenRouter Jev decisions.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dev:polling": "tsx scripts/polling.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test",
    "smoke": "tsx scripts/live-smoke.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260920.1",
    "@types/node": "^22.15.0",
    "dotenv": "^17.2.2",
    "tsx": "^4.19.0",
    "typescript": "^5.8.3",
    "vitest": "^5.0.1",
    "wrangler": "^4.33.0"
  }
}
